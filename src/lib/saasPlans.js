/** @deprecated Import from `./subscriptionPlans` — kept for existing import paths. */
export {
  DEFAULT_TRIAL_DAYS,
  BILLING_CURRENCY,
  UNLIMITED,
  PAID_PLAN_CODES,
  CANONICAL_PLANS,
  isUnlimited,
  formatLimit,
  isContactSalesPlan,
  normalizePlanCode,
  getPlanByCode,
  planPriceLabel,
  formatPlanPriceKes,
  checkPlanLimit,
  mergeCanonicalPlans,
  safePublicPlan,
  paidPublicPlans,
} from "./subscriptionPlans.js";
