import RolesPermissionsPanel from "../components/RolesPermissionsPanel";

/** Standalone route — same panel used inside Settings. */
export default function RolesPermissions() {
  return <RolesPermissionsPanel />;
}
